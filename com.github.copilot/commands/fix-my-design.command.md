---
description: Interrogate an existing design and rebuild what fails — not surface patching.
---

Load and follow the `fix-my-design` skill. The user's message after the slash invocation names the target: the design, component, or file to fix.

When the procedure presents findings, stop and hold until the user approves changes — the approval gate is the skill's own discipline, not something the agent may waive.